# Changesets

This folder tracks changes to promptoro that haven't been released yet. After making a change, run `npx changeset`. It asks how big the change is, and what to write in the changelog.

When your code lands on `main`, the changelog updates and a new version goes out to npm automatically.